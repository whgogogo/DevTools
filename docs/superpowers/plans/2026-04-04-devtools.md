# DevTools 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一款基于 Shell/Bash 的开发环境 CLI 工具，支持节点/容器搜索跳转、Jar包替换、远程Debug、数据库连接、日志查询等核心功能。

**Architecture:** 模块化 Shell 脚本项目。主入口 `bin/devtools` 负责加载 lib 下的功能模块并渲染交互式菜单。各模块通过 `source` 引入，共享 `common.sh`（颜色/SSH/kubectl封装）和 `config.sh`（配置读写）。功能1（search.sh）是其他所有功能的基础依赖。

**Tech Stack:** Bash 4+, kubectl, docker, sshpass/expect（SSH自动登录）, socat（端口转发）

**Spec:** `docs/superpowers/specs/2026-04-04-devtools-design.md`

---

## 文件结构总览

```
devtools/
├── install.sh                    # Task 11: 一键安装
├── uninstall.sh                  # Task 11: 卸载
├── bin/
│   └── devtools                  # Task 4: 主入口+菜单
├── lib/
│   ├── common.sh                 # Task 1: 公共函数
│   ├── i18n.sh                   # Task 2: 国际化
│   ├── config.sh                 # Task 3: 配置管理
│   ├── search.sh                 # Task 5: 搜索跳转
│   ├── replace.sh                # Task 6: Jar包替换
│   ├── debug.sh                  # Task 7: 远程Debug
│   ├── database.sh               # Task 8: 数据库连接
│   └── log.sh                    # Task 9: 日志查询
├── conf/
│   └── config.template           # Task 10: 配置模板
└── tests/
    ├── test_helper.bash          # Task 1: 测试辅助
    ├── common_test.sh            # Task 1
    ├── config_test.sh            # Task 3
    ├── i18n_test.sh              # Task 2
    ├── search_test.sh            # Task 5
    ├── replace_test.sh           # Task 6
    ├── debug_test.sh             # Task 7
    ├── database_test.sh          # Task 8
    └── log_test.sh               # Task 9
```

---

### Task 1: 项目脚手架 + common.sh 公共函数

**Files:**
- Create: `lib/common.sh`
- Create: `tests/test_helper.bash`
- Create: `tests/common_test.sh`

- [ ] **Step 1: 创建项目目录结构**

```bash
mkdir -p bin lib conf tests
```

- [ ] **Step 2: 编写 tests/test_helper.bash**

```bash
#!/usr/bin/env bash
# 测试辅助函数
# 用法: source tests/test_helper.bash

export DEVTOOLS_ROOT_DIR="${DEVTOOLS_ROOT_DIR:-$(cd "$(dirname "$0")/.." && pwd)}"
export TEST_TMPDIR=""

setup() {
    TEST_TMPDIR=$(mktemp -d)
    export DEVTOOLS_CONFIG_DIR="$TEST_TMPDIR/config"
    export DEVTOOLS_HOME="$TEST_TMPDIR"
    mkdir -p "$DEVTOOLS_CONFIG_DIR"
}

teardown() {
    [[ -n "$TEST_TMPDIR" && -d "$TEST_TMPDIR" ]] && rm -rf "$TEST_TMPDIR"
}

# 断言两个字符串相等
assert_eq() {
    local expected="$1" actual="$2" msg="${3:-assertion failed}"
    if [[ "$expected" != "$actual" ]]; then
        echo "FAIL: $msg"
        echo "  expected: '$expected'"
        echo "  actual:   '$actual'"
        return 1
    fi
}

# 断言字符串包含子串
assert_contains() {
    local haystack="$1" needle="$2" msg="${3:-assertion failed}"
    if [[ "$haystack" != *"$needle"* ]]; then
        echo "FAIL: $msg"
        echo "  haystack: '$haystack'"
        echo "  needle:   '$needle'"
        return 1
    fi
}

# 断言命令成功（退出码0）
assert_exit_code() {
    local expected="$1" actual="$2" msg="${3:-exit code mismatch}"
    if [[ "$expected" != "$actual" ]]; then
        echo "FAIL: $msg"
        echo "  expected exit code: $expected"
        echo "  actual exit code:   $actual"
        return 1
    fi
}

# 运行单个测试文件
run_test_file() {
    local file="$1"
    echo "=== Running: $file ==="
    source "$file"
    echo "=== Passed: $file ==="
}
```

- [ ] **Step 3: 编写 lib/common.sh**

```bash
#!/usr/bin/env bash
# common.sh - 公共函数：颜色输出、菜单渲染、SSH封装、kubectl封装
# 所有模块共享的基础函数库

# ============================================================
# 颜色定义
# ============================================================
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 颜色输出函数
print_red()    { echo -e "${RED}$1${NC}"; }
print_green()  { echo -e "${GREEN}$1${NC}"; }
print_yellow() { echo -e "${YELLOW}$1${NC}"; }
print_blue()   { echo -e "${BLUE}$1${NC}"; }
print_cyan()   { echo -e "${CYAN}$1${NC}"; }
print_bold()   { echo -e "${BOLD}$1${NC}"; }

# ============================================================
# 菜单渲染
# ============================================================

# 渲染带框的标题
print_header() {
    local title="$1"
    echo ""
    print_bold "╔══════════════════════════════════════╗"
    print_bold "║       ${title}"
    print_bold "╠══════════════════════════════════════╣"
}

# 渲染菜单项
print_menu_item() {
    local num="$1" text="$2"
    printf "  ${CYAN}[%s]${NC}  %s\n" "$num" "$text"
}

# 渲染菜单底框
print_footer() {
    print_bold "╚══════════════════════════════════════╝"
}

# 读取用户选择（带默认值）
read_input() {
    local prompt="$1" default="${2:-}"
    if [[ -n "$default" ]]; then
        read -rp "$(echo -e "${CYAN}${prompt}${NC} [默认: ${default}]: ")" input
        echo "${input:-$default}"
    else
        read -rp "$(echo -e "${CYAN}${prompt}${NC}: ")" input
        echo "$input"
    fi
}

# 读取用户确认（y/n）
read_confirm() {
    local prompt="${1:-确认执行？}"
    local default="${2:-y}"
    local yn
    read -rp "$(echo -e "${YELLOW}${prompt} (y/n) [默认: ${default}]: ${NC}")" yn
    yn="${yn:-$default}"
    [[ "$yn" =~ ^[Yy]$ ]]
}

# ============================================================
# SSH 封装
# ============================================================

# 通过 SSH 在远程节点执行命令（使用 sshpass 自动登录）
# 用法: ssh_exec <user> <host> <password> <command...>
ssh_exec() {
    local user="$1" host="$2" password="$3"
    shift 3
    sshpass -p "$password" ssh -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        -o ConnectTimeout=10 \
        "${user}@${host}" "$@"
}

# 通过 SSH 交互式登录到远程节点
# 用法: ssh_login <user> <host> <password>
ssh_login() {
    local user="$1" host="$2" password="$3"
    sshpass -p "$password" ssh -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        "${user}@${host}"
}

# SCP 传输文件（使用 paas 用户）
# 用法: scp_transfer <user> <password> <src_file> <host>:<dst_path>
scp_transfer() {
    local user="$1" password="$2" src="$3" dst="$4"
    sshpass -p "$password" scp -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        "$src" "${user}@${dst}"
}

# 通过 SSH 建立 SSH 端口转发
# 用法: ssh_port_forward <user> <host> <password> <local_port> <remote_port>
ssh_port_forward() {
    local user="$1" host="$2" password="$3" local_port="$4" remote_port="$5"
    sshpass -p "$password" ssh -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        -N -L "${local_port}:localhost:${remote_port}" \
        -f "${user}@${host}"
    echo $!
}

# ============================================================
# kubectl 封装
# ============================================================

# 获取所有节点信息
# 输出格式: "节点名|节点IP|节点状态" 每行一条
kubectl_get_nodes() {
    kubectl get nodes -o custom-columns=NAME:.metadata.name,IP:.status.addresses[0].address,STATUS:.status.conditions[-1].type --no-headers 2>/dev/null \
        | awk '{print $1"|"$2"|"$3}'
}

# 获取所有 Pod 信息
# 输出格式: "命名空间|Pod名|节点名|容器状态" 每行一条
kubectl_get_pods() {
    kubectl get pods -A -o custom-columns=NAMESPACE:.metadata.namespace,NAME:.metadata.name,NODE:.spec.nodeName,STATUS:.status.phase --no-headers 2>/dev/null \
        | awk '{print $1"|"$2"|"$3"|"$4}'
}

# 获取 Pod 中的容器列表
# 用法: kubectl_get_containers <pod_name> <namespace>
kubectl_get_containers() {
    local pod="$1" ns="${2:-default}"
    kubectl get pod "$pod" -n "$ns" -o jsonpath='{range .spec.containers[*]}{.name}{"\n"}{end}' 2>/dev/null
}

# ============================================================
# 架构检测
# ============================================================

# 检测当前系统架构
# 返回: x86 或 arm
detect_arch() {
    local arch
    arch=$(uname -m)
    case "$arch" in
        x86_64|amd64) echo "x86" ;;
        aarch64|arm64) echo "arm" ;;
        *) echo "x86" ;; # 默认x86
    esac
}

# ============================================================
# 文件大小格式化
# ============================================================

# 将字节数转为人类可读格式
format_size() {
    local bytes="$1"
    if [[ $bytes -ge 1073741824 ]]; then
        echo "$(echo "scale=1; $bytes/1073741824" | bc)GB"
    elif [[ $bytes -ge 1048576 ]]; then
        echo "$(echo "scale=1; $bytes/1048576" | bc)MB"
    elif [[ $bytes -ge 1024 ]]; then
        echo "$(echo "scale=1; $bytes/1024" | bc)KB"
    else
        echo "${bytes}B"
    fi
}

# ============================================================
# 时间戳生成
# ============================================================

# 生成备份用的时间戳，格式: YYYYMMDD_HHMMSS
gen_timestamp() {
    date "+%Y%m%d_%H%M%S"
}
```

- [ ] **Step 4: 编写 tests/common_test.sh**

```bash
#!/usr/bin/env bash
# common.sh 单元测试

source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"

setup

# --- 颜色函数测试 ---
echo "--- 测试: detect_arch ---"
arch=$(detect_arch)
if [[ "$arch" == "x86" || "$arch" == "arm" ]]; then
    echo "PASS: detect_arch 返回 $arch"
else
    echo "FAIL: detect_arch 返回未知值 $arch"
fi

# --- gen_timestamp 测试 ---
echo "--- 测试: gen_timestamp ---"
ts=$(gen_timestamp)
if [[ "$ts" =~ ^[0-9]{8}_[0-9]{6}$ ]]; then
    echo "PASS: gen_timestamp 格式正确 $ts"
else
    echo "FAIL: gen_timestamp 格式错误 $ts"
fi

# --- format_size 测试 ---
echo "--- 测试: format_size ---"
result=$(format_size 1073741824)
assert_contains "$result" "GB" "1GB格式化"
echo "PASS: format_size 1GB -> $result"

result=$(format_size 1048576)
assert_contains "$result" "MB" "1MB格式化"
echo "PASS: format_size 1MB -> $result"

result=$(format_size 1024)
assert_contains "$result" "KB" "1KB格式化"
echo "PASS: format_size 1KB -> $result"

echo ""
echo "=== common_test.sh 全部完成 ==="
```

- [ ] **Step 5: 运行测试**

```bash
bash tests/common_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 6: 提交**

```bash
git add lib/common.sh tests/test_helper.bash tests/common_test.sh
git commit -m "feat: add common.sh with color output, menu rendering, SSH/kubectl wrappers and tests"
```

---

### Task 2: i18n.sh 国际化模块

**Files:**
- Create: `lib/i18n.sh`
- Create: `tests/i18n_test.sh`

- [ ] **Step 1: 编写 lib/i18n.sh**

```bash
#!/usr/bin/env bash
# i18n.sh - 国际化模块
# 根据 ~/.devtools/config 中 LANG 配置返回中/英文字符串

# 当前语言（默认中文）
I18N_LANG="zh_CN"

# 加载语言设置（由 config.sh 调用）
i18n_init() {
    I18N_LANG="${1:-zh_CN}"
}

# ============================================================
# 中文消息
# ============================================================
declare -A MSG_ZH
MSG_ZH[app_title]="DevTools v1.0"
MSG_ZH[menu_search]="搜索节点/容器并跳转"
MSG_ZH[menu_replace]="一键更换Jar包"
MSG_ZH[menu_debug]="一键开启远程Debug"
MSG_ZH[menu_database]="连接数据库"
MSG_ZH[menu_log]="查询DAC Spark日志"
MSG_ZH[menu_config]="配置管理"
MSG_ZH[menu_help]="帮助"
MSG_ZH[menu_quit]="退出"
MSG_ZH[select_prompt]="请选择"
MSG_ZH[search_keyword]="请输入搜索关键词（节点名或容器名）"
MSG_ZH[search_empty]="未找到匹配结果，请重新输入"
MSG_ZH[search_header]="序号  节点名称          容器名称                    节点IP"
MSG_ZH[search_back]="(0 返回主菜单)"
MSG_ZH[replace_select_jar]="请选择要替换的jar包"
MSG_ZH[replace_found_jars]="在 /home/paas 下发现以下jar包："
MSG_ZH[replace_backup]="正在备份原jar包..."
MSG_ZH[replace_com_dir]="检测到com目录，正在改名..."
MSG_ZH[replace_transfer]="正在传输jar包到节点..."
MSG_ZH[replace_chown]="正在修改文件属组..."
MSG_ZH[replace_docker_cp]="正在替换容器内jar包..."
MSG_ZH[replace_cleanup]="正在清理临时文件..."
MSG_ZH[replace_restart]="正在重启容器..."
MSG_ZH[replace_done]="Jar包替换完成，容器已重启"
MSG_ZH[debug_port_prompt]="请配置Debug端口"
MSG_ZH[debug_port_jump]="跳板机端口"
MSG_ZH[debug_port_node]="节点端口"
MSG_ZH[debug_port_container]="容器内端口"
MSG_ZH[debug_detect_jdk]="正在检测JDK版本..."
MSG_ZH[debug_jdk8]="检测到 JDK8"
MSG_ZH[debug_jdk21]="检测到 JDK21"
MSG_ZH[debug_exists]="检测到start.sh中已有Debug参数:"
MSG_ZH[debug_use_existing]="使用已有端口继续后续操作"
MSG_ZH[debug_replace_param]="替换为新的Debug参数"
MSG_ZH[debug_cancel]="取消操作"
MSG_ZH[debug_disabling_hc]="正在禁用健康检查..."
MSG_ZH[debug_port_forward]="正在建立端口映射..."
MSG_ZH[debug_ready]="远程Debug已就绪！"
MSG_ZH[debug_idea_hint]="请在IDEA中配置: Run -> Edit Configurations -> Remote JVM Debug"
MSG_ZH[debug_cleanup]="正在清理Debug配置..."
MSG_ZH[debug_restored]="Debug配置已清理，容器已重启"
MSG_ZH[db_select]="请选择要连接的数据库"
MSG_ZH[db_connecting]="正在连接数据库..."
MSG_ZH[log_searching]="正在搜索DAC节点..."
MSG_ZH[log_viewing]="正在查看日志..."
MSG_ZH[log_filter_prompt]="请输入过滤关键词（直接回车查看全部）"
MSG_ZH[log_mode_prompt]="查看模式"
MSG_ZH[log_mode_follow]="实时跟踪(follow)"
MSG_ZH[log_mode_recent]="最近500行"
MSG_ZH[config_select]="请选择配置项"
MSG_ZH[config_view]="查看当前配置"
MSG_ZH[config_lang]="修改语言设置"
MSG_ZH[config_ssh]="修改SSH凭据"
MSG_ZH[config_hc]="修改健康检查脚本列表"
MSG_ZH[config_db]="修改数据库关键词与连接命令"
MSG_ZH[config_log_path]="修改日志路径"
MSG_ZH[help_search]="[1] 搜索: 通过关键词模糊搜索节点/容器，选中后一键SSH跳转"
MSG_ZH[help_replace]="[2] 替换: 扫描本地jar包，选择后一键替换到容器内并重启"
MSG_ZH[help_debug]="[3] Debug: 自动配置JVM参数、禁用健康检查、建立两级端口转发"
MSG_ZH[help_database]="[4] 数据库: 选择数据库类型后自动跳转并执行连接命令"
MSG_ZH[help_log]="[5] 日志: 搜索DAC节点并查看spark.log日志"
MSG_ZH[help_config]="[c] 配置: 管理SSH凭据、数据库连接命令、健康检查脚本等"
MSG_ZH[operation_confirm]="确认执行？"
MSG_ZH[success]="操作成功"
MSG_ZH[failed]="操作失败"
MSG_ZH[back_to_menu]="(0 返回)"

# ============================================================
# 英文消息
# ============================================================
declare -A MSG_EN
MSG_EN[app_title]="DevTools v1.0"
MSG_EN[menu_search]="Search Nodes/Containers"
MSG_EN[menu_replace]="Replace Jar Package"
MSG_EN[menu_debug]="Enable Remote Debug"
MSG_EN[menu_database]="Connect to Database"
MSG_EN[menu_log]="View DAC Spark Log"
MSG_EN[menu_config]="Configuration"
MSG_EN[menu_help]="Help"
MSG_EN[menu_quit]="Quit"
MSG_EN[select_prompt]="Please select"
MSG_EN[search_keyword]="Enter search keyword (node name or container name)"
MSG_EN[search_empty]="No results found, please try again"
MSG_EN[search_header]="No.   Node Name          Container Name                Node IP"
MSG_EN[search_back]="(0 Back to main menu)"
MSG_EN[replace_select_jar]="Select jar to replace"
MSG_EN[replace_found_jars]="Found following jars in /home/paas:"
MSG_EN[replace_backup]="Backing up original jar..."
MSG_EN[replace_com_dir]="Detected com directory, renaming..."
MSG_EN[replace_transfer]="Transferring jar to node..."
MSG_EN[replace_chown]="Changing file ownership..."
MSG_EN[replace_docker_cp]="Replacing jar in container..."
MSG_EN[replace_cleanup]="Cleaning up temp files..."
MSG_EN[replace_restart]="Restarting container..."
MSG_EN[replace_done]="Jar replaced, container restarted"
MSG_EN[debug_port_prompt]="Configure Debug ports"
MSG_EN[debug_port_jump]="Jump server port"
MSG_EN[debug_port_node]="Node port"
MSG_EN[debug_port_container]="Container port"
MSG_EN[debug_detect_jdk]="Detecting JDK version..."
MSG_EN[debug_jdk8]="Detected JDK8"
MSG_EN[debug_jdk21]="Detected JDK21"
MSG_EN[debug_exists]="Found existing debug params in start.sh:"
MSG_EN[debug_use_existing]="Use existing port and continue"
MSG_EN[debug_replace_param]="Replace with new debug params"
MSG_EN[debug_cancel]="Cancel"
MSG_EN[debug_disabling_hc]="Disabling health check..."
MSG_EN[debug_port_forward]="Setting up port forwarding..."
MSG_EN[debug_ready]="Remote Debug ready!"
MSG_EN[debug_idea_hint]="In IDEA: Run -> Edit Configurations -> Remote JVM Debug"
MSG_EN[debug_cleanup]="Cleaning up debug config..."
MSG_EN[debug_restored]="Debug config cleaned, container restarted"
MSG_EN[db_select]="Select database to connect"
MSG_EN[db_connecting]="Connecting to database..."
MSG_EN[log_searching]="Searching for DAC node..."
MSG_EN[log_viewing]="Viewing logs..."
MSG_EN[log_filter_prompt]="Enter filter keyword (Enter for all)"
MSG_EN[log_mode_prompt]="View mode"
MSG_EN[log_mode_follow]="Follow (real-time)"
MSG_EN[log_mode_recent]="Recent 500 lines"
MSG_EN[config_select]="Select config item"
MSG_EN[config_view]="View current config"
MSG_EN[config_lang]="Change language setting"
MSG_EN[config_ssh]="Change SSH credentials"
MSG_EN[config_hc]="Change health check scripts"
MSG_EN[config_db]="Change database keywords and commands"
MSG_EN[config_log_path]="Change log path"
MSG_EN[help_search]="[1] Search: Fuzzy search nodes/containers by keyword, SSH jump on select"
MSG_EN[help_replace]="[2] Replace: Scan local jars, replace into container and restart"
MSG_EN[help_debug]="[3] Debug: Auto-configure JVM params, disable health check, two-level port forward"
MSG_EN[help_database]="[4] Database: Select DB type, auto-jump and execute connect command"
MSG_EN[help_log]="[5] Log: Search DAC node and view spark.log"
MSG_EN[help_config]="[c] Config: Manage SSH credentials, DB commands, health check scripts"
MSG_EN[operation_confirm]="Confirm?"
MSG_EN[success]="Operation succeeded"
MSG_EN[failed]="Operation failed"
MSG_EN[back_to_menu]="(0 Back)"

# ============================================================
# 消息获取函数
# ============================================================

# 获取国际化消息
# 用法: msg "key"
msg() {
    local key="$1"
    if [[ "$I18N_LANG" == "en_US" ]]; then
        echo "${MSG_EN[$key]:-$key}"
    else
        echo "${MSG_ZH[$key]:-$key}"
    fi
}
```

- [ ] **Step 2: 编写 tests/i18n_test.sh**

```bash
#!/usr/bin/env bash
source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"
source "$DEVTOOLS_ROOT_DIR/lib/i18n.sh"

setup

echo "--- 测试: 中文默认语言 ---"
i18n_init "zh_CN"
result=$(msg "menu_search")
assert_eq "$result" "搜索节点/容器并跳转" "中文菜单搜索"
echo "PASS: 中文输出正确"

echo "--- 测试: 英文语言 ---"
i18n_init "en_US"
result=$(msg "menu_search")
assert_eq "$result" "Search Nodes/Containers" "英文菜单搜索"
echo "PASS: 英文输出正确"

echo "--- 测试: 不存在的key返回key本身 ---"
i18n_init "zh_CN"
result=$(msg "nonexistent_key_xyz")
assert_eq "$result" "nonexistent_key_xyz" "不存在的key"
echo "PASS: 不存在的key返回原值"

echo "--- 测试: 默认语言 ---"
i18n_init ""
result=$(msg "menu_quit")
assert_eq "$result" "退出" "默认中文退出"
echo "PASS: 默认语言为中文"

echo ""
echo "=== i18n_test.sh 全部完成 ==="
```

- [ ] **Step 3: 运行测试**

```bash
bash tests/i18n_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add lib/i18n.sh tests/i18n_test.sh
git commit -m "feat: add i18n module with zh_CN/en_US support"
```

---

### Task 3: config.sh 配置管理模块

**Files:**
- Create: `lib/config.sh`
- Create: `tests/config_test.sh`

- [ ] **Step 1: 编写 lib/config.sh**

```bash
#!/usr/bin/env bash
# config.sh - 配置管理模块
# 负责读写 ~/.devtools/config 配置文件

CONFIG_FILE="${HOME}/.devtools/config"

# ============================================================
# 配置加载
# ============================================================

# 加载配置文件（仅全局变量部分，不含 section）
# 用法: config_load
config_load() {
    if [[ ! -f "$CONFIG_FILE" ]]; then
        return 1
    fi
    # 读取非 section 部分（非 [xxx] 开头，非 CONNECT_CMD<<EOF 块）
    local in_heredoc=0
    while IFS= read -r line || [[ -n "$line" ]]; do
        # 跳过空行和注释
        [[ -z "$line" || "$line" =~ ^[[:space:]]*# ]] && continue
        # 检测 heredoc 开始
        if [[ "$line" =~ ^CONNECT_CMD<<EOF ]]; then
            in_heredoc=1
            continue
        fi
        # 检测 heredoc 结束
        if [[ "$in_heredoc" -eq 1 && "$line" == "EOF" ]]; then
            in_heredoc=0
            continue
        fi
        if [[ "$in_heredoc" -eq 1 ]]; then
            continue
        fi
        # 跳过 section 标题
        [[ "$line" =~ ^\[ ]] && continue
        # 解析 KEY=VALUE
        if [[ "$line" =~ ^([A-Z_]+)=(.*) ]]; then
            local key="${BASH_REMATCH[1]}"
            local val="${BASH_REMATCH[2]}"
            # 去除前后空白
            val=$(echo "$val" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
            export "$key=$val"
        fi
    done < "$CONFIG_FILE"
    return 0
}

# ============================================================
# 配置读取
# ============================================================

# 获取全局配置值
# 用法: config_get "KEY"
config_get() {
    local key="$1"
    echo "${!key}"
}

# 获取数据库 section 中的值
# 用法: config_get_section <section> <key>
# 例如: config_get_section "gaussv1" "KEYWORD" -> gaussdb-v1
config_get_section() {
    local section="$1" key="$2"
    if [[ ! -f "$CONFIG_FILE" ]]; then
        return 1
    fi
    local in_section=0 in_heredoc=0
    local heredoc_key=""
    while IFS= read -r line || [[ -n "$line" ]]; do
        # 处理 heredoc 结束
        if [[ "$in_heredoc" -eq 1 && "$line" == "EOF" ]]; then
            in_heredoc=0
            heredoc_key=""
            continue
        fi
        if [[ "$in_heredoc" -eq 1 ]]; then
            continue
        fi
        # 检测 heredoc 开始（在 section 内）
        if [[ "$in_section" -eq 1 && "$line" =~ ^([A-Z_]+)<<EOF ]]; then
            heredoc_key="${BASH_REMATCH[1]}"
            if [[ "$heredoc_key" == "$key" ]]; then
                # 读取多行命令直到 EOF
                local cmd_lines=()
                while IFS= read -r cmd_line && [[ "$cmd_line" != "EOF" ]]; do
                    cmd_lines+=("$cmd_line")
                done
                printf '%s\n' "${cmd_lines[@]}"
                return 0
            fi
            in_heredoc=1
            continue
        fi
        # 检测 section 开始
        if [[ "$line" == "[$section]" ]]; then
            in_section=1
            continue
        fi
        # 检测 section 结束
        if [[ "$in_section" -eq 1 && "$line" =~ ^\[ ]] && [[ "$line" != "[$section]" ]]; then
            break
        fi
        # 读取 section 内的 KEY=VALUE
        if [[ "$in_section" -eq 1 && "$line" =~ ^${key}=(.*) ]]; then
            echo "${BASH_REMATCH[1]}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
            return 0
        fi
    done < "$CONFIG_FILE"
    return 1
}

# ============================================================
# 配置写入
# ============================================================

# 设置全局配置值
# 用法: config_set "KEY" "VALUE"
config_set() {
    local key="$1" value="$2"
    if [[ -f "$CONFIG_FILE" ]]; then
        if grep -q "^${key}=" "$CONFIG_FILE"; then
            sed -i "s|^${key}=.*|${key}=${value}|" "$CONFIG_FILE"
        else
            echo "${key}=${value}" >> "$CONFIG_FILE"
        fi
    fi
}

# ============================================================
# 获取所有数据库 section 名称
# ============================================================

# 返回配置文件中所有 section 名称（用于动态生成数据库菜单）
# 用法: config_get_db_sections
config_get_db_sections() {
    if [[ ! -f "$CONFIG_FILE" ]]; then
        return 1
    fi
    grep '^\[' "$CONFIG_FILE" | tr -d '[]' | sort
}

# ============================================================
# 获取健康检查脚本列表（逗号分隔转数组）
# ============================================================

# 用法: config_get_hc_scripts
# 返回: 空格分隔的脚本名列表
config_get_hc_scripts() {
    local scripts="${HEALTH_CHECK_SCRIPTS:-}"
    echo "${scripts//,/ }"
}
```

- [ ] **Step 2: 编写 tests/config_test.sh**

```bash
#!/usr/bin/env bash
source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"
source "$DEVTOOLS_ROOT_DIR/lib/config.sh"

setup

# 创建测试配置文件
cat > "$CONFIG_FILE" << 'CONFIG_EOF'
LANG=zh_CN
SSH_USER_PAAS=paas
SSH_PASS_PAAS=test123
HEALTH_CHECK_SCRIPTS=health_check.sh,check_health.sh
DAC_SPARK_LOG_PATH=/var/log/dac/spark.log

[gaussv1]
KEYWORD=gaussdb-v1
CONNECT_CMD<<EOF
source /home/paas/gauss_env.sh
gsql -d mydb -p 5432
EOF

[gaussv3]
KEYWORD=gaussdb-v3
CONNECT_CMD=gsql -d mydb -p 5432

[carbon]
KEYWORD=carbon
CONNECT_CMD<<EOF
export JAVA_HOME=/opt/java
beeline -u "jdbc:hive2://localhost:10000/mydb"
EOF
CONFIG_EOF

echo "--- 测试: config_load ---"
config_load
assert_eq "$SSH_USER_PAAS" "paas" "加载SSH用户"
assert_eq "$SSH_PASS_PAAS" "test123" "加载SSH密码"
echo "PASS: config_load 正确加载全局变量"

echo "--- 测试: config_get ---"
result=$(config_get "LANG")
assert_eq "$result" "zh_CN" "获取LANG"
echo "PASS: config_get 正确获取值"

echo "--- 测试: config_get_section 单行值 ---"
result=$(config_get_section "gaussv3" "KEYWORD")
assert_eq "$result" "gaussdb-v3" "获取section单行值"
echo "PASS: section单行值正确"

echo "--- 测试: config_get_section 多行命令 ---"
result=$(config_get_section "gaussv1" "CONNECT_CMD")
assert_contains "$result" "source /home/paas/gauss_env.sh" "多行命令第一行"
assert_contains "$result" "gsql -d mydb -p 5432" "多行命令第二行"
echo "PASS: section多行命令正确"

echo "--- 测试: config_get_db_sections ---"
sections=$(config_get_db_sections)
assert_contains "$sections" "gaussv1" "数据库section列表"
assert_contains "$sections" "carbon" "数据库section列表包含carbon"
echo "PASS: 数据库section列表正确: $sections"

echo "--- 测试: config_get_hc_scripts ---"
scripts=$(config_get_hc_scripts)
assert_contains "$scripts" "health_check.sh" "健康检查脚本列表"
assert_contains "$scripts" "check_health.sh" "健康检查脚本列表"
echo "PASS: 健康检查脚本列表正确: $scripts"

echo "--- 测试: config_set ---"
config_set "LANG" "en_US"
result=$(grep "^LANG=" "$CONFIG_FILE")
assert_eq "$result" "LANG=en_US" "修改配置值"
echo "PASS: config_set 正确修改"

echo ""
echo "=== config_test.sh 全部完成 ==="
```

- [ ] **Step 3: 运行测试**

```bash
bash tests/config_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add lib/config.sh tests/config_test.sh
git commit -m "feat: add config module with section parsing and multi-line command support"
```

---

### Task 4: bin/devtools 主入口 + 主菜单

**Files:**
- Create: `bin/devtools`

- [ ] **Step 1: 编写 bin/devtools**

```bash
#!/usr/bin/env bash
# devtools - 主入口脚本
# 加载所有模块并展示交互式主菜单

set -euo pipefail

# 定位项目根目录
DEVTOOLS_HOME="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
LIB_DIR="${DEVTOOLS_HOME}/lib"

# 加载核心模块
source "${LIB_DIR}/common.sh"
source "${LIB_DIR}/i18n.sh"
source "${LIB_DIR}/config.sh"

# 加载功能模块
source "${LIB_DIR}/search.sh"
source "${LIB_DIR}/replace.sh"
source "${LIB_DIR}/debug.sh"
source "${LIB_DIR}/database.sh"
source "${LIB_DIR}/log.sh"

# ============================================================
# 初始化
# ============================================================

init() {
    # 加载配置
    if ! config_load; then
        print_red "$(msg 'failed'): 配置文件不存在"
        print_yellow "请执行 install.sh 或手动创建 ~/.devtools/config"
        exit 1
    fi
    # 初始化国际化
    i18n_init "${LANG:-zh_CN}"
}

# ============================================================
# 主菜单
# ============================================================

show_main_menu() {
    echo ""
    print_header "$(msg 'app_title')"
    print_menu_item "1" "$(msg 'menu_search')"
    print_menu_item "2" "$(msg 'menu_replace')"
    print_menu_item "3" "$(msg 'menu_debug')"
    print_menu_item "4" "$(msg 'menu_database')"
    print_menu_item "5" "$(msg 'menu_log')"
    print_menu_item "c" "$(msg 'menu_config')"
    print_menu_item "h" "$(msg 'menu_help')"
    print_menu_item "q" "$(msg 'menu_quit')"
    print_footer
}

# ============================================================
# 帮助信息
# ============================================================

show_help() {
    echo ""
    print_header "$(msg 'app_title') - $(msg 'menu_help')"
    echo ""
    print_green "$(msg 'help_search')"
    print_green "$(msg 'help_replace')"
    print_green "$(msg 'help_debug')"
    print_green "$(msg 'help_database')"
    print_green "$(msg 'help_log')"
    print_green "$(msg 'help_config')"
    print_footer
    echo ""
}

# ============================================================
# 配置管理子菜单
# ============================================================

show_config_menu() {
    while true; do
        echo ""
        print_header "$(msg 'menu_config')"
        print_menu_item "1" "$(msg 'config_view')"
        print_menu_item "2" "$(msg 'config_lang')"
        print_menu_item "3" "$(msg 'config_ssh')"
        print_menu_item "4" "$(msg 'config_hc')"
        print_menu_item "5" "$(msg 'config_db')"
        print_menu_item "6" "$(msg 'config_log_path')"
        print_menu_item "0" "$(msg 'menu_quit')"
        print_footer
        choice=$(read_input "$(msg 'select_prompt')")
        case "$choice" in
            1)
                echo ""
                print_cyan "=== $(msg 'config_view') ==="
                cat "$CONFIG_FILE"
                ;;
            2)
                echo ""
                echo "zh_CN / en_US"
                new_lang=$(read_input "$(msg 'config_lang')")
                config_set "LANG" "$new_lang"
                i18n_init "$new_lang"
                print_green "$(msg 'success')"
                ;;
            3)
                echo ""
                new_user=$(read_input "SSH_USER_PAAS")
                config_set "SSH_USER_PAAS" "$new_user"
                new_pass=$(read_input "SSH_PASS_PAAS")
                config_set "SSH_PASS_PAAS" "$new_pass"
                new_user=$(read_input "SSH_USER_ROOT")
                config_set "SSH_USER_ROOT" "$new_user"
                new_pass=$(read_input "SSH_PASS_ROOT")
                config_set "SSH_PASS_ROOT" "$new_pass"
                export SSH_USER_PAAS="$new_user"
                export SSH_PASS_PAAS="$new_pass"
                export SSH_USER_ROOT="$new_user"
                export SSH_PASS_ROOT="$new_pass"
                print_green "$(msg 'success')"
                ;;
            4)
                echo ""
                print_yellow "当前值: ${HEALTH_CHECK_SCRIPTS:-}"
                new_hc=$(read_input "$(msg 'config_hc')" "${HEALTH_CHECK_SCRIPTS:-}")
                config_set "HEALTH_CHECK_SCRIPTS" "$new_hc"
                export HEALTH_CHECK_SCRIPTS="$new_hc"
                print_green "$(msg 'success')"
                ;;
            5)
                print_yellow "请直接编辑配置文件中的数据库 section: $CONFIG_FILE"
                ;;
            6)
                echo ""
                new_path=$(read_input "$(msg 'config_log_path')" "${DAC_SPARK_LOG_PATH:-}")
                config_set "DAC_SPARK_LOG_PATH" "$new_path"
                export DAC_SPARK_LOG_PATH="$new_path"
                print_green "$(msg 'success')"
                ;;
            0|q)
                break
                ;;
        esac
    done
}

# ============================================================
# 主循环
# ============================================================

main() {
    init
    while true; do
        show_main_menu
        choice=$(read_input "$(msg 'select_prompt')")
        case "$choice" in
            1) do_search ;;
            2) do_replace ;;
            3) do_debug ;;
            4) do_database ;;
            5) do_log ;;
            c|C) show_config_menu ;;
            h|H) show_help ;;
            q|Q) exit 0 ;;
            *) print_red "无效选择，请重新输入" ;;
        esac
    done
}

main "$@"
```

- [ ] **Step 2: 设置执行权限**

```bash
chmod +x bin/devtools
```

- [ ] **Step 3: 提交**

```bash
git add bin/devtools
git commit -m "feat: add main entry with interactive menu, help, and config management"
```

---

### Task 5: search.sh 搜索跳转模块（核心）

**Files:**
- Create: `lib/search.sh`
- Create: `tests/search_test.sh`

- [ ] **Step 1: 编写 lib/search.sh**

```bash
#!/usr/bin/env bash
# search.sh - 功能1：节点/容器搜索与跳转
# 核心能力模块，其他功能模块依赖此模块

# ============================================================
# 内部：构建搜索结果列表
# 结果存储在全局数组 SEARCH_RESULTS 中
# 格式: "节点名|容器名|节点IP"
# ============================================================

SEARCH_RESULTS=()

# 解析 kubectl 输出，构建搜索结果
# 用法: _build_search_results
_build_search_results() {
    SEARCH_RESULTS=()

    # 获取节点信息（名称|IP|状态）
    local nodes_raw
    nodes_raw=$(kubectl_get_nodes)
    # 获取 pod 信息（命名空间|Pod名|节点名|状态）
    local pods_raw
    pods_raw=$(kubectl_get_pods)

    # 构建节点IP查找表
    declare -A node_ip_map
    while IFS='|' read -r name ip status; do
        [[ -n "$name" && -n "$ip" ]] && node_ip_map["$name"]="$ip"
    done <<< "$nodes_raw"

    # 遍历 pod，构建搜索结果
    while IFS='|' read -r ns pod node status; do
        [[ -z "$pod" ]] && continue
        local node_ip="${node_ip_map[$node]:-$node}"
        # 只取 pod 名的第一段（去掉 hash 后缀）作为容器标识
        local container_label="$pod"
        SEARCH_RESULTS+=("${node}|${container_label}|${node_ip}")
    done <<< "$pods_raw"
}

# ============================================================
# 搜索并展示结果
# 用法: do_search
# ============================================================

do_search() {
    while true; do
        keyword=$(read_input "$(msg 'search_keyword')")
        [[ -z "$keyword" ]] && return

        # 构建搜索结果
        _build_search_results

        # 过滤匹配结果
        local matched=()
        for entry in "${SEARCH_RESULTS[@]}"; do
            IFS='|' read -r node container ip <<< "$entry"
            if [[ "$node" == *"$keyword"* || "$container" == *"$keyword"* ]]; then
                matched+=("$entry")
            fi
        done

        # 去重（按 节点名+容器名 组合）
        local unique=()
        declare -A seen
        for entry in "${matched[@]}"; do
            IFS='|' read -r node container ip <<< "$entry"
            local key="${node}|${container}"
            if [[ -z "${seen[$key]}" ]]; then
                seen["$key"]=1
                unique+=("$entry")
            fi
        done

        # 结果为空
        if [[ ${#unique[@]} -eq 0 ]]; then
            print_yellow "$(msg 'search_empty')"
            continue
        fi

        # 展示结果
        echo ""
        printf "  %-4s  %-18s  %-30s  %-16s\n" "序号" "节点名称" "容器名称" "节点IP"
        printf "  %-4s  %-18s  %-30s  %-16s\n" "----" "──────────" "──────────────────────" "────────"
        local idx=1
        for entry in "${unique[@]}"; do
            IFS='|' read -r node container ip <<< "$entry"
            printf "  %-4s  %-18s  %-30s  %-16s\n" "$idx" "$node" "$container" "$ip"
            ((idx++))
        done
        echo ""
        print_yellow "$(msg 'search_back')"
        echo ""

        # 用户选择
        choice=$(read_input "$(msg 'select_prompt')")
        [[ "$choice" == "0" || -z "$choice" ]] && return

        if [[ "$choice" =~ ^[0-9]+$ && "$choice" -ge 1 && "$choice" -le ${#unique[@]} ]]; then
            local selected="${unique[$((choice-1))]}"
            IFS='|' read -r node container ip <<< "$selected"
            print_green "正在跳转到 ${node} ..."
            ssh_login "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS"
        else
            print_red "无效选择"
        fi
    done
}

# ============================================================
# 供其他模块调用的搜索函数
# 返回选中的结果，存入全局变量
# ============================================================

# 搜索并选择一个目标（供其他模块调用）
# 设置全局变量: SELECTED_NODE, SELECTED_CONTAINER, SELECTED_IP
# 用法: search_select <keyword> [--allow-empty]
# 返回: 0=选中, 1=取消/空结果
search_select() {
    local keyword="$1"
    local allow_empty=false
    [[ "${2:-}" == "--allow-empty" ]] && allow_empty=true

    _build_search_results

    local matched=()
    for entry in "${SEARCH_RESULTS[@]}"; do
        IFS='|' read -r node container ip <<< "$entry"
        if [[ "$node" == *"$keyword"* || "$container" == *"$keyword"* ]]; then
            matched+=("$entry")
        fi
    done

    # 去重
    local unique=()
    declare -A seen
    for entry in "${matched[@]}"; do
        IFS='|' read -r node container ip <<< "$entry"
        local key="${node}|${container}"
        if [[ -z "${seen[$key]}" ]]; then
            seen["$key"]=1
            unique+=("$entry")
        fi
    done

    if [[ ${#unique[@]} -eq 0 ]]; then
        $allow_empty && return 1
        print_yellow "$(msg 'search_empty')"
        return 1
    fi

    # 展示
    echo ""
    printf "  %-4s  %-18s  %-30s  %-16s\n" "序号" "节点名称" "容器名称" "节点IP"
    printf "  %-4s  %-18s  %-30s  %-16s\n" "----" "──────────" "──────────────────────" "────────"
    local idx=1
    for entry in "${unique[@]}"; do
        IFS='|' read -r node container ip <<< "$entry"
        printf "  %-4s  %-18s  %-30s  %-16s\n" "$idx" "$node" "$container" "$ip"
        ((idx++))
    done
    echo ""
    print_yellow "$(msg 'search_back')"

    choice=$(read_input "$(msg 'select_prompt')")
    [[ "$choice" == "0" || -z "$choice" ]] && return 1

    if [[ "$choice" =~ ^[0-9]+$ && "$choice" -ge 1 && "$choice" -le ${#unique[@]} ]]; then
        local selected="${unique[$((choice-1))]}"
        IFS='|' read -r node container ip <<< "$selected"
        SELECTED_NODE="$node"
        SELECTED_CONTAINER="$container"
        SELECTED_IP="$ip"
        return 0
    fi
    return 1
}
```

- [ ] **Step 2: 编写 tests/search_test.sh**

```bash
#!/usr/bin/env bash
source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"
source "$DEVTOOLS_ROOT_DIR/lib/i18n.sh"
source "$DEVTOOLS_ROOT_DIR/lib/config.sh"
source "$DEVTOOLS_ROOT_DIR/lib/search.sh"

setup
i18n_init "zh_CN"

echo "--- 测试: kubectl_get_nodes 解析 ---"
# 模拟 kubectl 输出
mock_nodes="master|10.0.0.1|Ready
worker1|10.0.0.2|Ready
worker2|10.0.0.3|Ready"

echo "--- 测试: kubectl_get_pods 解析 ---"
mock_pods="default|api-server-6d8f9b|master|Running
default|api-gateway-3a2c1|worker1|Running
default|data-service-7e4b2|worker1|Running"

echo "--- 测试: 搜索结果去重逻辑 ---"
# 直接测试 SEARCH_RESULTS 数组的去重
declare -A seen
unique=()
test_entries=("worker1|api-gateway-3a2c1|10.0.0.2" "worker1|data-service-7e4b2|10.0.0.2" "worker1|api-gateway-3a2c1|10.0.0.2")
for entry in "${test_entries[@]}"; do
    IFS='|' read -r node container ip <<< "$entry"
    key="${node}|${container}"
    if [[ -z "${seen[$key]}" ]]; then
        seen["$key"]=1
        unique+=("$entry")
    fi
done
assert_eq "${#unique[@]}" "2" "去重后应剩2条"
echo "PASS: 去重逻辑正确，${#unique[@]} 条结果"

echo ""
echo "=== search_test.sh 全部完成 ==="
```

- [ ] **Step 3: 运行测试**

```bash
bash tests/search_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add lib/search.sh tests/search_test.sh
git commit -m "feat: add search module with fuzzy match, dedup, and reusable search_select function"
```

---

### Task 6: replace.sh Jar包替换模块

**Files:**
- Create: `lib/replace.sh`
- Create: `tests/replace_test.sh`

- [ ] **Step 1: 编写 lib/replace.sh**

```bash
#!/usr/bin/env bash
# replace.sh - 功能2：一键更换Jar包

do_replace() {
    echo ""
    keyword=$(read_input "$(msg 'search_keyword')")
    [[ -z "$keyword" ]] && return

    # 搜索选择目标容器
    if ! search_select "$keyword"; then
        return
    fi

    local node="$SELECTED_NODE"
    local container="$SELECTED_CONTAINER"
    local ip="$SELECTED_IP"

    # 扫描 /home/paas 下的 jar 包
    print_cyan "$(msg 'replace_found_jars')"
    echo ""

    local jars=()
    local jar_sizes=()
    local jar_paths=()

    # 扫描 /home/paas 及一级子目录
    while IFS= read -r -d '' jar_file; do
        local rel_path="${jar_file#/home/paas/}"
        local size
        size=$(stat -c%s "$jar_file" 2>/dev/null || stat -f%z "$jar_file" 2>/dev/null)
        local human_size
        human_size=$(format_size "$size")
        jars+=("$rel_path")
        jar_sizes+=("$human_size")
        jar_paths+=("$jar_file")
    done < <(find /home/paas -maxdepth 2 -name "*.jar" -type f -print0 2>/dev/null | sort -z)

    if [[ ${#jars[@]} -eq 0 ]]; then
        print_red "在 /home/paas 下未找到任何jar包"
        return
    fi

    # 展示 jar 包列表
    printf "  %-4s  %-45s  %-10s\n" "序号" "文件名" "大小"
    printf "  %-4s  %-45s  %-10s\n" "----" "─────────────────────────────────────────" "─────"
    local idx=1
    for i in "${!jars[@]}"; do
        printf "  %-4s  %-45s  %-10s\n" "$idx" "${jars[$i]}" "${jar_sizes[$i]}"
        ((idx++))
    done
    echo ""
    print_yellow "$(msg 'back_to_menu')"

    choice=$(read_input "$(msg 'replace_select_jar')")
    [[ "$choice" == "0" || -z "$choice" ]] && return

    if [[ ! "$choice" =~ ^[0-9]+$ || "$choice" -lt 1 || "$choice" -gt ${#jars[@]} ]]; then
        print_red "无效选择"
        return
    fi

    local selected_idx=$((choice-1))
    local jar_path="${jar_paths[$selected_idx]}"
    local jar_name
    jar_name=$(basename "$jar_path")

    if ! read_confirm "$(msg 'operation_confirm')"; then
        return
    fi

    # 获取容器ID（从容器名提取或通过docker查找）
    local container_id
    container_id=$(ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "docker ps --format '{{.ID}} {{.Names}}' | grep '$container' | awk '{print \$1}' | head -1")

    if [[ -z "$container_id" ]]; then
        print_red "未找到容器: $container"
        return
    fi

    # Step 1: 备份容器内原jar包
    print_cyan "$(msg 'replace_backup')"
    local timestamp
    timestamp=$(gen_timestamp)
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "docker exec $container_id bash -c 'jar_files=\$(find / -maxdepth 4 -name \"*.jar\" -path \"*/app/*\" 2>/dev/null | head -1); if [ -n \"\$jar_files\" ]; then cp \$jar_files \${jar_files}.bak.$timestamp; fi'"
    echo "done"

    # Step 2: 处理 com 目录
    print_cyan "$(msg 'replace_com_dir')"
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "docker exec $container_id bash -c 'com_dirs=\$(find / -maxdepth 4 -type d -name \"com\" -path \"*/app/*\" 2>/dev/null | head -1); if [ -n \"\$com_dirs\" ]; then mv \$com_dirs \${com_dirs}.bak.$timestamp; fi'"
    echo "done"

    # Step 3: SCP传输到节点（paas用户，保持原始文件名）
    print_cyan "$(msg 'replace_transfer')"
    scp_transfer "$SSH_USER_PAAS" "$SSH_PASS_PAAS" "$jar_path" "${ip}:/home/paas/${jar_name}"

    # Step 4: 修改属组
    print_cyan "$(msg 'replace_chown')"
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "docker exec $container_id bash -c 'target_dir=\$(dirname \$(find / -maxdepth 4 -name \"*.jar\" -path \"*/app/*\" 2>/dev/null | head -1)); target_user=\$(stat -c \"%U\" \$target_dir 2>/dev/null || echo root); chown \$target_user:\$target_user /home/paas/${jar_name}'"

    # Step 5: docker cp 替换
    print_cyan "$(msg 'replace_docker_cp')"
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "target_path=\$(docker exec $container_id find / -maxdepth 4 -name \"*.jar\" -path \"*/app/*\" 2>/dev/null | head -1); docker cp /home/paas/${jar_name} $container_id:\$target_path"

    # Step 6: 清理临时文件
    print_cyan "$(msg 'replace_cleanup')"
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" "rm -f /home/paas/${jar_name}"

    # Step 7: 重启容器
    print_cyan "$(msg 'replace_restart')"
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" "docker restart $container_id"

    print_green "$(msg 'replace_done')"
}
```

- [ ] **Step 2: 编写 tests/replace_test.sh**

```bash
#!/usr/bin/env bash
source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"

setup

echo "--- 测试: jar包扫描逻辑 ---"
# 创建模拟 jar 文件
mkdir -p "$TEST_TMPDIR/paas/zhangsan" "$TEST_TMPDIR/paas/lisi"
touch "$TEST_TMPDIR/paas/app.jar"
touch "$TEST_TMPDIR/paas/zhangsan/gateway.jar"
touch "$TEST_TMPDIR/paas/lisi/service.jar"

# 测试 find 命令能正确找到
result=$(find "$TEST_TMPDIR/paas" -maxdepth 2 -name "*.jar" -type f | sort)
count=$(echo "$result" | grep -c ".jar")
assert_eq "$count" "3" "应找到3个jar包"
echo "PASS: jar包扫描找到 $count 个文件"

echo "--- 测试: format_size ---"
result=$(format_size 52428800)
assert_contains "$result" "MB" "50MB文件"
echo "PASS: 50MB -> $result"

echo ""
echo "=== replace_test.sh 全部完成 ==="
```

- [ ] **Step 3: 运行测试**

```bash
bash tests/replace_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add lib/replace.sh tests/replace_test.sh
git commit -m "feat: add jar replace module with backup, com dir handling, SCP transfer, and container restart"
```

---

### Task 7: debug.sh 远程Debug模块

**Files:**
- Create: `lib/debug.sh`
- Create: `tests/debug_test.sh`

- [ ] **Step 1: 编写 lib/debug.sh**

```bash
#!/usr/bin/env bash
# debug.sh - 功能3：一键开启远程Debug
# 支持两级端口转发、JDK8/21适配、防重复参数、健康检查禁用

# Debug会话状态文件（用于退出清理）
DEBUG_STATE_FILE="/tmp/devtools_debug_state_$$"

do_debug() {
    echo ""
    keyword=$(read_input "$(msg 'search_keyword')")
    [[ -z "$keyword" ]] && return

    # 搜索选择目标容器
    if ! search_select "$keyword"; then
        return
    fi

    local node="$SELECTED_NODE"
    local container="$SELECTED_CONTAINER"
    local ip="$SELECTED_IP"

    # 获取容器ID
    local container_id
    container_id=$(ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "docker ps --format '{{.ID}} {{.Names}}' | grep '$container' | awk '{print \$1}' | head -1")

    if [[ -z "$container_id" ]]; then
        print_red "未找到容器: $container"
        return
    fi

    # ---- 配置三个端口 ----
    echo ""
    print_cyan "$(msg 'debug_port_prompt')"
    local port_a port_b port_c
    port_a=$(read_input "$(msg 'debug_port_jump')" "5005")
    port_b=$(read_input "$(msg 'debug_port_node')" "5006")
    port_c=$(read_input "$(msg 'debug_port_container')" "5007")

    # ---- 检测JDK版本 ----
    print_cyan "$(msg 'debug_detect_jdk')"
    local java_ver_output
    java_ver_output=$(ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "docker exec $container_id java -version 2>&1" || true)

    local jdk_version="8"
    if echo "$java_ver_output" | grep -q "21\."; then
        jdk_version="21"
        print_green "$(msg 'debug_jdk21')"
    else
        print_green "$(msg 'debug_jdk8')"
    fi

    # ---- 生成Debug JVM参数 ----
    local debug_param
    if [[ "$jdk_version" == "21" ]]; then
        debug_param="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:${port_c}"
    else
        debug_param="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=${port_c}"
    fi

    # ---- 防重复检查 ----
    local existing_param
    existing_param=$(ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "docker exec $container_id grep -o 'agentlib:jdwp[^ ]*' /app/start.sh 2>/dev/null" || true)

    if [[ -n "$existing_param" ]]; then
        # 提取已有端口
        local existing_port
        existing_port=$(echo "$existing_param" | grep -oP 'address=?\K[0-9]+' | head -1)
        if [[ -z "$existing_port" ]]; then
            existing_port=$(echo "$existing_param" | grep -oP 'address=\*?:\K[0-9]+' | head -1)
        fi

        print_yellow "$(msg 'debug_exists')"
        echo "  $existing_param"
        echo ""
        echo "  [1] $(msg 'debug_use_existing') ($existing_port)"
        echo "  [2] $(msg 'debug_replace_param') ($port_c)"
        echo "  [0] $(msg 'debug_cancel')"
        echo ""
        dup_choice=$(read_input "$(msg 'select_prompt')")

        case "$dup_choice" in
            1)
                # 使用已有端口，调整 port_c
                port_c="$existing_port"
                ;;
            2)
                # 替换新参数
                ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
                    "docker exec $container_id sed -i 's/agentlib:jdwp[^ ]*/agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=${jdk_version == "21" ? "*:" : ""}${port_c}/' /app/start.sh"
                ;;
            0|*)
                return
                ;;
        esac
    else
        # 在 start.sh 中追加 debug 参数
        ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
            "docker exec $container_id sed -i '/^java /s/$/ ${debug_param}/' /app/start.sh"
    fi

    # ---- 禁用健康检查 ----
    print_cyan "$(msg 'debug_disabling_hc')"
    local hc_scripts
    hc_scripts=$(config_get_hc_scripts)
    for hc_script in $hc_scripts; do
        ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
            "docker exec $container_id bash -c 'if [ -f /app/${hc_script} ]; then sed -i \"1i exit 0\" /app/${hc_script}; fi'" 2>/dev/null || true
    done

    # ---- 重启容器使参数生效 ----
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" "docker restart $container_id"
    sleep 3

    # ---- 建立端口映射 ----
    print_cyan "$(msg 'debug_port_forward')"

    # 第一级: 容器端口 -> 节点端口
    local arch
    arch=$(ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" "uname -m" 2>/dev/null || echo "x86_64")

    # 获取容器内服务的实际监听端口映射
    # 使用 socat 做端口转发（x86/arm通用）
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
        "nohup socat TCP-LISTEN:${port_b},fork,reuseaddr TCP:localhost:${port_c} > /dev/null 2>&1 & echo \$!" > /tmp/devtools_socat_${port_b}.pid

    # 第二级: 节点端口 -> 跳板机端口（SSH端口转发）
    local ssh_pid
    ssh_pid=$(ssh_port_forward "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" "$port_a" "$port_b")

    # ---- 保存状态用于清理 ----
    local jump_ip
    jump_ip=$(hostname -I 2>/dev/null | awk '{print $1}' || echo "127.0.0.1")
    cat > "$DEBUG_STATE_FILE" << STATE_EOF
NODE_IP=$ip
CONTAINER_ID=$container_id
PORT_A=$port_a
PORT_B=$port_b
PORT_C=$port_c
SSH_PID=$ssh_pid
HC_SCRIPTS=$hc_scripts
STATE_EOF

    # ---- 输出连接信息 ----
    echo ""
    print_green "$(msg 'debug_ready')"
    echo "  跳板机IP: $jump_ip"
    echo "  跳板机端口: $port_a"
    print_cyan "$(msg 'debug_idea_hint')"
    echo ""
    print_yellow "按 Enter 退出Debug模式并清理配置..."
    read -r

    # ---- 退出清理 ----
    _debug_cleanup
}

# 清理Debug配置
_debug_cleanup() {
    if [[ ! -f "$DEBUG_STATE_FILE" ]]; then
        return
    fi

    source "$DEBUG_STATE_FILE"
    print_cyan "$(msg 'debug_cleanup')"

    # 关闭SSH端口转发
    if [[ -n "$SSH_PID" ]]; then
        kill "$SSH_PID" 2>/dev/null || true
    fi

    # 关闭节点上的socat
    ssh_exec "$SSH_USER_PAAS" "$NODE_IP" "$SSH_PASS_PAAS" \
        "kill \$(cat /tmp/devtools_socat_${PORT_B}.pid 2>/dev/null) 2>/dev/null; rm -f /tmp/devtools_socat_${PORT_B}.pid" 2>/dev/null || true

    # 恢复健康检查脚本
    for hc_script in $HC_SCRIPTS; do
        ssh_exec "$SSH_USER_PAAS" "$NODE_IP" "$SSH_PASS_PAAS" \
            "docker exec $CONTAINER_ID bash -c 'if [ -f /app/${hc_script} ]; then sed -i \"1d\" /app/${hc_script}; fi'" 2>/dev/null || true
    done

    # 移除 start.sh 中的 debug 参数
    ssh_exec "$SSH_USER_PAAS" "$NODE_IP" "$SSH_PASS_PAAS" \
        "docker exec $CONTAINER_ID sed -i 's/ -agentlib:jdwp[^ ]*//' /app/start.sh" 2>/dev/null || true

    # 重启容器恢复正常
    ssh_exec "$SSH_USER_PAAS" "$NODE_IP" "$SSH_PASS_PAAS" "docker restart $CONTAINER_ID"

    # 清理状态文件
    rm -f "$DEBUG_STATE_FILE"

    print_green "$(msg 'debug_restored')"
}
```

- [ ] **Step 2: 编写 tests/debug_test.sh**

```bash
#!/usr/bin/env bash
source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"

setup

echo "--- 测试: JDK版本检测逻辑 ---"
# 模拟 java -version 输出
jdk8_output='openjdk version "1.8.0_302"'
jdk21_output='openjdk version "21.0.1" 2023-10-17'

if echo "$jdk8_output" | grep -q "21\."; then
    echo "FAIL: JDK8 误判为21"
else
    echo "PASS: JDK8 正确识别"
fi

if echo "$jdk21_output" | grep -q "21\."; then
    echo "PASS: JDK21 正确识别"
else
    echo "FAIL: JDK21 未识别"
fi

echo "--- 测试: Debug参数生成 ---"
port_c="5007"
jdk8_param="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=${port_c}"
jdk21_param="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:${port_c}"

assert_contains "$jdk8_param" "address=5007" "JDK8参数格式"
assert_contains "$jdk21_param" "address=*:5007" "JDK21参数格式（带*）"
echo "PASS: JVM参数格式正确"

echo "--- 测试: 从已有参数提取端口 ---"
existing="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5009"
existing_port=$(echo "$existing" | grep -oP 'address=\K[0-9]+' | head -1)
assert_eq "$existing_port" "5009" "提取已有端口"
echo "PASS: 已有端口提取正确: $existing_port"

existing2="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5010"
existing_port2=$(echo "$existing2" | grep -oP 'address=\*?:\K[0-9]+' | head -1)
assert_eq "$existing_port2" "5010" "提取已有端口（带*前缀）"
echo "PASS: 已有端口提取正确（带*）: $existing_port2"

echo ""
echo "=== debug_test.sh 全部完成 ==="
```

- [ ] **Step 3: 运行测试**

```bash
bash tests/debug_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add lib/debug.sh tests/debug_test.sh
git commit -m "feat: add debug module with two-level port forwarding, JDK8/21 support, and auto-cleanup"
```

---

### Task 8: database.sh 数据库连接模块

**Files:**
- Create: `lib/database.sh`
- Create: `tests/database_test.sh`

- [ ] **Step 1: 编写 lib/database.sh**

```bash
#!/usr/bin/env bash
# database.sh - 功能4：数据库连接
# 从配置文件动态读取数据库类型，搜索定位节点，执行连接命令

do_database() {
    echo ""

    # 获取所有数据库 section
    local sections
    sections=$(config_get_db_sections)

    if [[ -z "$sections" ]]; then
        print_red "未在配置文件中找到数据库配置"
        return
    fi

    # 展示数据库列表
    local section_arr=()
    local idx=1
    while IFS= read -r section; do
        [[ -z "$section" ]] && continue
        section_arr+=("$section")
        print_menu_item "$idx" "$section"
        ((idx++))
    done <<< "$sections"

    echo ""
    print_yellow "$(msg 'back_to_menu')"
    choice=$(read_input "$(msg 'db_select')")
    [[ "$choice" == "0" || -z "$choice" ]] && return

    if [[ ! "$choice" =~ ^[0-9]+$ || "$choice" -lt 1 || "$choice" -gt ${#section_arr[@]} ]]; then
        print_red "无效选择"
        return
    fi

    local selected_section="${section_arr[$((choice-1))]}"

    # 获取搜索关键词
    local keyword
    keyword=$(config_get_section "$selected_section" "KEYWORD")
    if [[ -z "$keyword" ]]; then
        print_red "未找到 $selected_section 的 KEYWORD 配置"
        return
    fi

    # 获取连接命令
    local connect_cmd
    connect_cmd=$(config_get_section "$selected_section" "CONNECT_CMD")
    if [[ -z "$connect_cmd" ]]; then
        print_red "未找到 $selected_section 的 CONNECT_CMD 配置"
        return
    fi

    # 搜索定位节点/容器
    print_cyan "$(msg 'db_connecting')"
    if ! search_select "$keyword" --allow-empty; then
        print_red "未找到匹配的节点/容器"
        return
    fi

    local ip="$SELECTED_IP"

    # 执行连接命令（可能为多行）
    print_green "正在连接 $selected_section @ $ip ..."
    echo ""
    ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" "$connect_cmd"
}
```

- [ ] **Step 2: 编写 tests/database_test.sh**

```bash
#!/usr/bin/env bash
source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"
source "$DEVTOOLS_ROOT_DIR/lib/config.sh"

setup

# 创建测试配置文件
cat > "$CONFIG_FILE" << 'CONFIG_EOF'
LANG=zh_CN
SSH_USER_PAAS=paas
SSH_PASS_PAAS=test123

[gaussv1]
KEYWORD=gaussdb-v1
CONNECT_CMD<<EOF
source /home/paas/gauss_env.sh
gsql -d mydb -p 5432
EOF

[carbon]
KEYWORD=carbon
CONNECT_CMD=beeline -u "jdbc:hive2://localhost:10000/mydb"
CONFIG_EOF

echo "--- 测试: config_get_db_sections ---"
sections=$(config_get_db_sections)
assert_contains "$sections" "gaussv1" "数据库列表"
assert_contains "$sections" "carbon" "数据库列表"
echo "PASS: 数据库列表: $sections"

echo "--- 测试: 获取单行连接命令 ---"
result=$(config_get_section "carbon" "CONNECT_CMD")
assert_contains "$result" "beeline" "单行连接命令"
echo "PASS: 单行命令: $result"

echo "--- 测试: 获取多行连接命令 ---"
result=$(config_get_section "gaussv1" "CONNECT_CMD")
assert_contains "$result" "source" "多行命令第一行"
assert_contains "$result" "gsql" "多行命令第二行"
echo "PASS: 多行命令正确"

echo ""
echo "=== database_test.sh 全部完成 ==="
```

- [ ] **Step 3: 运行测试**

```bash
bash tests/database_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add lib/database.sh tests/database_test.sh
git commit -m "feat: add database connection module with dynamic section reading and multi-line command support"
```

---

### Task 9: log.sh 日志查询模块

**Files:**
- Create: `lib/log.sh`
- Create: `tests/log_test.sh`

- [ ] **Step 1: 编写 lib/log.sh**

```bash
#!/usr/bin/env bash
# log.sh - 功能5：查询DAC Spark日志

do_log() {
    echo ""

    # 搜索DAC节点
    print_cyan "$(msg 'log_searching')"
    if ! search_select "dac" --allow-empty; then
        print_red "未找到DAC节点"
        return
    fi

    local ip="$SELECTED_IP"
    local log_path="${DAC_SPARK_LOG_PATH:-/var/log/dac/spark.log}"

    # 选择查看模式
    echo ""
    print_menu_item "1" "$(msg 'log_mode_follow')"
    print_menu_item "2" "$(msg 'log_mode_recent')"
    choice=$(read_input "$(msg 'log_mode_prompt')")
    [[ -z "$choice" ]] && return

    case "$choice" in
        1)
            # 实时跟踪
            filter=$(read_input "$(msg 'log_filter_prompt')")
            if [[ -n "$filter" ]]; then
                ssh_login "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
                    -t "tail -f $log_path | grep --color=always '$filter'"
            else
                ssh_login "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
                    -t "tail -f $log_path"
            fi
            ;;
        2)
            # 最近500行
            filter=$(read_input "$(msg 'log_filter_prompt')")
            if [[ -n "$filter" ]]; then
                ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
                    "tail -n 500 $log_path | grep --color=always '$filter'"
            else
                ssh_exec "$SSH_USER_PAAS" "$ip" "$SSH_PASS_PAAS" \
                    "tail -n 500 $log_path"
            fi
            ;;
    esac
}
```

- [ ] **Step 2: 编写 tests/log_test.sh**

```bash
#!/usr/bin/env bash
source "$(dirname "$0")/test_helper.bash"
source "$DEVTOOLS_ROOT_DIR/lib/common.sh"
source "$DEVTOOLS_ROOT_DIR/lib/config.sh"

setup

echo "--- 测试: 日志路径配置 ---"
cat > "$CONFIG_FILE" << 'EOF'
DAC_SPARK_LOG_PATH=/var/log/dac/spark.log
EOF
config_load
assert_eq "$DAC_SPARK_LOG_PATH" "/var/log/dac/spark.log" "日志路径加载"
echo "PASS: 日志路径正确"

echo ""
echo "=== log_test.sh 全部完成 ==="
```

- [ ] **Step 3: 运行测试**

```bash
bash tests/log_test.sh
```

预期输出: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add lib/log.sh tests/log_test.sh
git commit -m "feat: add DAC Spark log query module with follow and recent modes"
```

---

### Task 10: conf/config.template 配置模板

**Files:**
- Create: `conf/config.template`

- [ ] **Step 1: 编写 conf/config.template**

```ini
# ============================================
# DevTools 配置文件
# ============================================

# 语言设置: zh_CN | en_US
LANG=zh_CN

# --------------------------------------------
# SSH凭据（各节点通用）
# --------------------------------------------
SSH_USER_PAAS=paas
SSH_PASS_PAAS=your_paas_password
SSH_USER_ROOT=root
SSH_PASS_ROOT=your_root_password

# --------------------------------------------
# 健康检查脚本名称列表（逗号分隔）
# 用于功能3开启Debug时禁用健康检查
# --------------------------------------------
HEALTH_CHECK_SCRIPTS=health_check.sh,healthcheck.sh,check_health.sh

# --------------------------------------------
# DAC Spark日志路径
# --------------------------------------------
DAC_SPARK_LOG_PATH=/var/log/dac/spark.log

# --------------------------------------------
# 数据库配置
# KEYWORD: 用于搜索定位对应节点/容器的关键词
# CONNECT_CMD: 跳转到对应节点后执行的连接命令
#   单行命令: CONNECT_CMD=gsql -d mydb -p 5432
#   多行命令: 使用 <<EOF ... EOF 包裹
# --------------------------------------------

[gaussv1]
KEYWORD=gaussdb-v1
CONNECT_CMD<<EOF
source /home/paas/gauss_env.sh
gsql -d mydb -p 5432 -U myuser
EOF

[gaussv3]
KEYWORD=gaussdb-v3
CONNECT_CMD<<EOF
source /home/paas/gauss_env.sh
gsql -d mydb -p 5432 -U myuser
EOF

[carbon]
KEYWORD=carbon
CONNECT_CMD<<EOF
export JAVA_HOME=/opt/java
beeline -u "jdbc:hive2://localhost:10000/mydb"
EOF

[hudi]
KEYWORD=hudi
CONNECT_CMD<<EOF
cd /opt/hudi
./connect.sh
EOF
```

- [ ] **Step 2: 提交**

```bash
git add conf/config.template
git commit -m "feat: add config template with database sections and multi-line command examples"
```

---

### Task 11: install.sh + uninstall.sh 安装卸载脚本

**Files:**
- Create: `install.sh`
- Create: `uninstall.sh`

- [ ] **Step 1: 编写 install.sh**

```bash
#!/usr/bin/env bash
# install.sh - 一键安装脚本
# 在跳板机上执行，自动分发到集群所有节点

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
INSTALL_DIR="/opt/devtools"
CONFIG_DIR="${HOME}/.devtools"
CONFIG_FILE="${CONFIG_DIR}/config"

echo ""
echo "=========================================="
echo "  DevTools 一键安装"
echo "=========================================="
echo ""

# ---- 检查依赖 ----
for cmd in kubectl sshpass ssh scp; do
    if ! command -v "$cmd" &>/dev/null; then
        echo "错误: 缺少依赖 $cmd，请先安装"
        exit 1
    fi
done

# ---- 检测架构 ----
arch=$(uname -m)
echo "系统架构: $arch"

# ---- 获取集群节点列表 ----
echo "正在获取集群节点列表..."
node_lines=$(kubectl get nodes -o custom-columns=NAME:.metadata.name,IP:.status.addresses[0].address --no-headers 2>/dev/null)
if [[ -z "$node_lines" ]]; then
    echo "错误: 无法获取节点列表，请检查 kubectl 配置"
    exit 1
fi

# 提取所有节点IP
node_ips=()
while IFS= read -r line; do
    ip=$(echo "$line" | awk '{print $2}')
    [[ -n "$ip" ]] && node_ips+=("$ip")
done <<< "$node_lines"

echo "发现 ${#node_ips[@]} 个节点: ${node_ips[*]}"
echo ""

# ---- 读取SSH凭据 ----
read -rp "请输入 paas 用户密码: " -s paas_pass
echo ""
read -rp "请输入 root 用户密码: " -s root_pass
echo ""

# ---- 安装到本机（跳板机） ----
echo "正在安装到本机..."
sudo mkdir -p "$INSTALL_DIR"
sudo cp -r "${SCRIPT_DIR}/bin" "${SCRIPT_DIR}/lib" "${SCRIPT_DIR}/conf" "$INSTALL_DIR/"
sudo chmod +x "${INSTALL_DIR}/bin/devtools"
sudo ln -sf "${INSTALL_DIR}/bin/devtools" /usr/local/bin/devtools
echo "本机安装完成"

# ---- 分发到各节点 ----
echo ""
echo "正在分发到集群节点..."
for ip in "${node_ips[@]}"; do
    echo "  -> $ip ..."
    sshpass -p "$root_pass" ssh -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        "root@${ip}" "mkdir -p ${INSTALL_DIR}" 2>/dev/null

    sshpass -p "$root_pass" scp -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        -r "${SCRIPT_DIR}/bin" "${SCRIPT_DIR}/lib" "${SCRIPT_DIR}/conf" \
        "root@${ip}:${INSTALL_DIR}/" 2>/dev/null

    sshpass -p "$root_pass" ssh -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        "root@${ip}" "chmod +x ${INSTALL_DIR}/bin/devtools && ln -sf ${INSTALL_DIR}/bin/devtools /usr/local/bin/devtools" 2>/dev/null

    echo "  $ip 安装完成"
done

# ---- 生成配置文件 ----
echo ""
echo "正在生成配置文件..."
mkdir -p "$CONFIG_DIR"
if [[ ! -f "$CONFIG_FILE" ]]; then
    cp "${SCRIPT_DIR}/conf/config.template" "$CONFIG_FILE"
    # 写入SSH密码
    sed -i "s/SSH_PASS_PAAS=your_paas_password/SSH_PASS_PAAS=${paas_pass}/" "$CONFIG_FILE"
    sed -i "s/SSH_PASS_ROOT=your_root_password/SSH_PASS_ROOT=${root_pass}/" "$CONFIG_FILE"
    chmod 600 "$CONFIG_FILE"
    echo "配置文件已生成: $CONFIG_FILE"
else
    echo "配置文件已存在，跳过: $CONFIG_FILE"
fi

# ---- 安装完成 ----
echo ""
echo "=========================================="
echo "  安装完成！"
echo "  安装路径: $INSTALL_DIR"
echo "  配置文件: $CONFIG_FILE"
echo "  执行 'devtools' 启动工具"
echo "=========================================="
echo ""
```

- [ ] **Step 2: 编写 uninstall.sh**

```bash
#!/usr/bin/env bash
# uninstall.sh - 卸载脚本

set -euo pipefail

INSTALL_DIR="/opt/devtools"
CONFIG_DIR="${HOME}/.devtools"

echo ""
echo "=========================================="
echo "  DevTools 卸载"
echo "=========================================="
echo ""

# ---- 获取集群节点列表 ----
echo "正在获取集群节点列表..."
node_ips=()
while IFS= read -r ip; do
    [[ -n "$ip" ]] && node_ips+=("$ip")
done < <(kubectl get nodes -o custom-columns=IP:.status.addresses[0].address --no-headers 2>/dev/null)

if [[ ${#node_ips[@]} -eq 0 ]]; then
    echo "警告: 无法获取节点列表"
    echo "将仅清理本机"
    node_ips=()
fi

read -rp "请输入 root 用户密码: " -s root_pass
echo ""

# ---- 在各节点上卸载 ----
for ip in "${node_ips[@]}"; do
    echo "  -> 卸载 $ip ..."
    sshpass -p "$root_pass" ssh -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        "root@${ip}" "rm -rf ${INSTALL_DIR} && rm -f /usr/local/bin/devtools" 2>/dev/null
    echo "  $ip 卸载完成"
done

# ---- 本机卸载 ----
echo "正在卸载本机..."
sudo rm -rf "$INSTALL_DIR"
sudo rm -f /usr/local/bin/devtools
echo "本机卸载完成"

# ---- 询问是否删除配置 ----
echo ""
read -rp "是否删除配置文件 ${CONFIG_DIR}？(y/n) [默认: n]: " confirm
if [[ "$confirm" =~ ^[Yy]$ ]]; then
    rm -rf "$CONFIG_DIR"
    echo "配置文件已删除"
else
    echo "配置文件已保留: $CONFIG_DIR"
fi

echo ""
echo "卸载完成！"
echo ""
```

- [ ] **Step 3: 设置执行权限**

```bash
chmod +x install.sh uninstall.sh
```

- [ ] **Step 4: 提交**

```bash
git add install.sh uninstall.sh
git commit -m "feat: add install.sh (auto-distribute to cluster) and uninstall.sh"
```

---

## 自审检查

### Spec 覆盖

| 设计文档章节 | 对应 Task |
|---|---|
| 2. 项目结构 | Task 1 (目录创建) |
| 3. 配置文件设计 | Task 3 (config.sh) + Task 10 (config.template) |
| 4.0 主菜单 | Task 4 (bin/devtools) |
| 4.1 搜索跳转 | Task 5 (search.sh) |
| 4.2 Jar包替换 | Task 6 (replace.sh) |
| 4.3 远程Debug | Task 7 (debug.sh) |
| 4.4 数据库连接 | Task 8 (database.sh) |
| 4.5 DAC日志 | Task 9 (log.sh) |
| 4.6 配置管理 | Task 4 (show_config_menu) |
| 4.7 帮助 | Task 4 (show_help) |
| 5. 国际化 | Task 2 (i18n.sh) |
| 6. 安装机制 | Task 11 (install.sh + uninstall.sh) |

### 占位符扫描
无 TBD/TODO。所有代码步骤包含完整实现。

### 类型一致性
- 所有模块通过 `source` 加载，全局变量名一致：`SSH_USER_PAAS`, `SSH_PASS_PAAS`, `SSH_USER_ROOT`, `SSH_PASS_ROOT`, `CONFIG_FILE`, `HEALTH_CHECK_SCRIPTS`, `DAC_SPARK_LOG_PATH`
- `search_select` 在 Task 5 定义，被 Task 6/7/8/9 调用，接口一致：设置 `SELECTED_NODE`, `SELECTED_CONTAINER`, `SELECTED_IP`
- `config_get_section` 在 Task 3 定义，被 Task 8 调用，接口一致
